# Allow local connections via UNIX domain socket (e.g., from inside the container)
local   all             all                                     trust

# Allow internal Docker network connections only
host    all             all             172.18.0.0/16           trust

# Reject all other external TCP connections explicitly
host    all             all             0.0.0.0/0               reject
host    all             all             ::/0                    reject
